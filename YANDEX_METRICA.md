# Как получить номер счётчика Яндекс.Метрики

Номер счётчика — это ID вашей Метрики, который нужно подставить в `index.html` вместо `0`:

```js
var ym = 0; // ЗАМЕНИ НА СВОЙ НОМЕР СЧЁТЧИКА
```

## Шаги

1. **Войдите в Яндекс** — https://passport.yandex.ru (нужен аккаунт Яндекса).
2. **Откройте Метрику** — https://metrika.yandex.ru.
3. Нажмите **«+ Добавить счётчик»** (справа вверху).
4. Заполните форму:
   - **Имя счётчика** — например «uptime-studio-site»;
   - **Адрес сайта** — `https://uptimestudio.ru`;
   - таймзона и валюта — по желанию.
5. Нажмите **«Создать счётчик»**. Вам сразу покажут **номер счётчика** (обычно 6–10 цифр) и готовый код для вставки.
6. Скопируйте номер и подставьте его в `index.html` в `var ym = <номер>;`.
7. Задеплойте сайт и проверьте в Метрике вкладку **«Настройки → Проверка счётчика»** — статус должен стать «Работает».

## Где ещё посмотреть номер

- В списке счётчиков на https://metrika.yandex.ru — номер под названием счётчика.
- В URL страницы счётчика: `https://metrika.yandex.ru/counter?id=<номер>`.
